'use strict';

import api from './api';
import mongo from './mongo';
import security from './security';
import stormpath from './stormpath';
import swagger from './swagger';

export { api, mongo, security, stormpath, swagger };

export default {api, mongo, security, stormpath, swagger};