'use strict';

import api from './api';
import mongo from './mongo';
import stormpath from './stormpath';
import swagger from './swagger';

export { api, mongo, stormpath, swagger };

export default {api, mongo, stormpath, swagger};