'use strict';

import api from './api';
import stormpath from './stormpath';
import swagger from './swagger';
import aws from './aws';

export { api, stormpath, swagger };

export default {api, stormpath, swagger, aws};