'use strict';

import api from './api';
import stormpath from './stormpath';
import errors from './errors';
import swagger from './swagger';

export { api, stormpath, errors, swagger };

export default {api, stormpath, errors, swagger};